enum ActionTypeKeys {
  TEST_CALL = "OTHER_CALL" //,

  // This type is to handle third party and redux built in action types.
  // BUILT_IN = "BUILT_IN" TODO re-enable ?
}

export default ActionTypeKeys;
